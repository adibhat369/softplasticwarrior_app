<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.softplasticwarrior1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_spw"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_spw_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity
            android:name=".Challenge_1"
            android:label="@string/app_name" />
        <activity android:name=".Challenge_2">

        </activity>
        <activity
            android:name=".AboutAppVideo"
            android:label="@string/app_name" />
        <activity
            android:name=".TaskVideo"
            android:label="@string/app_name" />
    </application>

</manifest>